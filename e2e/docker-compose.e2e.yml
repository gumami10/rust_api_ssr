services:
  app:
    build:
      context: ..
      dockerfile: Dockerfile
    ports:
      - "3456:3000"
    environment:
      BIND_ADDRESS: 0.0.0.0:3000
      DATABASE_URL: sqlite:///app/e2e_test.db
      COOKIE_SECURE: "false"
      RUST_LOG: error
